as400release {
  installPhase = 'DOK'
  as400version = "#RELEASE_VERSION#"
  regMVL {
    mode = 'SUSP'  // SUSP or NORM, SUSP by default
  }
  packages {
    addPackages "#PACKAGE_NAME#"
  }
  releaseNotes {
    addText = "#PATCH_DESCRIPTION#"
    #FSD_LINK#
    #TASK_LINK#
    #BRD_LINK#
    #TESTS_LINK#
  }
}

project.ext.retailDescription = 'Settlement System.'
project.ext.taskID='#BUMBER_TASK#'

apply from: 'src/assembly/config.gradle'